services:
  - type: web
    name: nexus-agri-platform
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
